<% provide(:title, 'Usuarios') %>

<h1>Usuarios</h1>
<% if @users.empty? %>
<p>No hay usuarios que mostrar.</p>
<% else %>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.username, user %>
  </li>
  <% end %>
</ul>
<% end %>
