<% provide(:title, 'Usuarios') %>

<h1>Usuarios</h1>
<% if @users.empty? %>
<p>No hay usuarios que mostrar.</p>
<% else %>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= user.username %>
  </li>
  <% end %>
</ul>
<% end %>
